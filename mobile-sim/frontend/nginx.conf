server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Route all paths to index.html so React handles routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
